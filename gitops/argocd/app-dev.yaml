apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devops-app-prod
  namespace: argocd
spec:
  project: default
  # 1. Source: Where is the code located in Git?
  source:
    repoURL: https://github.com/erendemirer1/cloud-native-devops-platform.git
    targetRevision: HEAD
    path: charts
    helm:
      valueFiles:
        - values-prod.yaml

  # 2. Destination: Where to deploy inside Kubernetes?
  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  # 3. Sync Policy: Full automated GitOps and Self-Healing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
